# 批量出售代币命令指南

## 📋 命令列表

### 基础命令

#### 1. 模拟模式（推荐先测试）
```bash
# 使用 pnpm
pnpm batch-sell

# 使用 npm
npm run batch-sell

# 直接运行（推荐）
npx tsx src/batch-sell.ts
```

#### 2. 实盘模式（真实出售）
```bash
# 使用 pnpm
pnpm batch-sell-real

# 直接运行（推荐）
npx tsx src/batch-sell.ts --real
```

---

## 🎯 带参数的命令

### 设置最小价格

只出售价格 >= 指定价格的代币：

```bash
# 只出售价格 >= $0.1 的代币
npx tsx src/batch-sell.ts --min-price 0.1

# 实盘模式 + 最小价格
npx tsx src/batch-sell.ts --real --min-price 0.1
```

### 设置最大滑点

```bash
# 设置最大滑点为 5%
npx tsx src/batch-sell.ts --max-slippage 0.05

# 实盘模式 + 5% 滑点
npx tsx src/batch-sell.ts --real --max-slippage 0.05
```

### 设置交易延迟

```bash
# 每笔交易之间延迟 2 秒（默认 1 秒）
npx tsx src/batch-sell.ts --delay 2000

# 实盘模式 + 2 秒延迟
npx tsx src/batch-sell.ts --real --delay 2000
```

### 组合使用多个参数

```bash
# 实盘模式 + 最小价格 $0.1 + 5% 滑点 + 2 秒延迟
npx tsx src/batch-sell.ts --real --min-price 0.1 --max-slippage 0.05 --delay 2000

# 模拟模式 + 最小价格 $0.05 + 3% 滑点 + 1.5 秒延迟
npx tsx src/batch-sell.ts --min-price 0.05 --max-slippage 0.03 --delay 1500
```

---

## 📊 参数说明

| 参数 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| `--real` | 实盘模式（真实出售） | 模拟模式 | `--real` |
| `--min-price <价格>` | 最小出售价格（USD） | 0（无限制） | `--min-price 0.1` |
| `--max-slippage <滑点>` | 最大滑点（0.05 = 5%） | 0.03 (3%) | `--max-slippage 0.05` |
| `--delay <毫秒>` | 每笔交易之间的延迟 | 1000ms | `--delay 2000` |

---

## 🚀 常用命令组合

### 安全测试（推荐首次使用）

```bash
# 模拟模式，查看会出售哪些代币
npx tsx src/batch-sell.ts
```

### 快速出售所有持仓

```bash
# 实盘模式，无价格限制，默认滑点 3%，延迟 1 秒
npx tsx src/batch-sell.ts --real
```

### 只出售高价值代币

```bash
# 实盘模式，只出售价格 >= $0.5 的代币
npx tsx src/batch-sell.ts --real --min-price 0.5
```

### 保守出售（严格滑点控制）

```bash
# 实盘模式，最大滑点 1%，延迟 2 秒
npx tsx src/batch-sell.ts --real --max-slippage 0.01 --delay 2000
```

### 快速批量出售

```bash
# 实盘模式，最小价格 $0.05，延迟 0.5 秒（快速）
npx tsx src/batch-sell.ts --real --min-price 0.05 --delay 500
```

---

## ⚙️ 环境变量配置

也可以通过 `.env` 文件配置：

```env
# 私钥（必需）
POLYMARKET_PRIVATE_KEY=your_private_key

# 是否模拟模式（true = 模拟，false = 实盘）
DRY_RUN=true
```

环境变量优先级：
- 命令行参数 `--real` 会覆盖 `DRY_RUN=true`
- 如果命令行没有 `--real`，则使用 `DRY_RUN` 环境变量

---

## 📝 命令执行示例

### 示例 1：测试运行

```bash
# 查看会出售哪些持仓（不会真实执行）
npx tsx src/batch-sell.ts
```

输出示例：
```
═══════════════════════════════════════════════════
   Polymarket 批量出售代币工具
═══════════════════════════════════════════════════

📋 配置信息：
   模式: 🔍 模拟模式 (Dry Run)
   最小价格: 无限制
   最大滑点: 3%
   交易延迟: 1000ms

💰 钱包地址: 0x...
...
```

### 示例 2：真实出售

```bash
# 真实出售所有持仓
npx tsx src/batch-sell.ts --real
```

### 示例 3：只出售高价代币

```bash
# 只出售价格 >= $0.2 的代币
npx tsx src/batch-sell.ts --real --min-price 0.2
```

---

## ⚠️ 重要提示

1. **首次使用建议先测试**：
   ```bash
   npx tsx src/batch-sell.ts  # 模拟模式，不会真实出售
   ```

2. **实盘模式会真实出售**：
   ```bash
   npx tsx src/batch-sell.ts --real  # 会真实执行交易
   ```

3. **确保钱包有足够余额**：用于 Gas 费用

4. **建议设置最小价格**：过滤掉价值太低的代币

---

## 🔍 故障排查

### 如果命令找不到文件

```bash
# 检查文件是否存在
ls -la src/batch-sell.ts

# 检查当前目录
pwd

# 应该在项目根目录
cd ~/poly-copy-trading
```

### 如果 SDK 导入错误

```bash
# 重新安装依赖
pnpm install
# 或
npm install
```

### 如果权限错误

```bash
# 确保文件有执行权限
chmod +x src/batch-sell.ts
```

---

## 📚 更多信息

- 详细使用指南：`BATCH_SELL_GUIDE.md`
- 服务器更新指南：`SERVER_UPDATE_GUIDE.md`
- 完整文件内容：`batch-sell-complete.txt`
