# 服务器安装详细流程

本指南提供在服务器上安装和部署 `poly-copy-trading` 项目的完整步骤。

## 📋 目录

1. [前置要求](#前置要求)
2. [第一步：连接到服务器](#第一步连接到服务器)
3. [第二步：系统环境准备](#第二步系统环境准备)
4. [第三步：克隆项目](#第三步克隆项目)
5. [第四步：安装项目依赖](#第四步安装项目依赖)
6. [第五步：配置环境变量](#第五步配置环境变量)
7. [第六步：测试运行](#第六步测试运行)
8. [第七步：使用 PM2 后台运行](#第七步使用-pm2-后台运行)
9. [第八步：设置开机自启](#第八步设置开机自启)
10. [常见问题解决](#常见问题解决)

---

## 前置要求

- ✅ 已购买云服务器（腾讯云、阿里云、AWS 等）
- ✅ 已获取服务器 IP 地址和 root 密码（或 SSH 密钥）
- ✅ 已准备好 Polymarket 私钥
- ✅ 服务器操作系统：Ubuntu 20.04+ / CentOS 7+ / Debian 10+

---

## 第一步：连接到服务器

### 1.1 Windows 用户（使用 PowerShell 或 CMD）

```bash
# 使用密码连接
ssh root@你的服务器IP

# 示例
ssh root@123.456.789.0

# 如果使用密钥文件
ssh -i 你的密钥文件.pem root@你的服务器IP
```

### 1.2 Mac/Linux 用户

```bash
ssh root@你的服务器IP
```

### 1.3 验证连接

连接成功后，你应该看到类似这样的提示：

```
Welcome to Ubuntu 20.04 LTS
...
root@your-server:~#
```

---

## 第二步：系统环境准备

### 2.1 更新系统（推荐）

**Ubuntu/Debian:**
```bash
apt update && apt upgrade -y
```

**CentOS/RHEL:**
```bash
yum update -y
```

### 2.2 安装 Node.js 20.x LTS

**Ubuntu/Debian:**
```bash
# 安装 curl（如果未安装）
apt install -y curl

# 安装 Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# 验证安装
node --version
npm --version
```

**CentOS/RHEL:**
```bash
# 安装 curl（如果未安装）
yum install -y curl

# 安装 Node.js 20.x
curl -fsSL https://rpm.nodesource.com/setup_20.x | bash -
yum install -y nodejs

# 验证安装
node --version
npm --version
```

**预期输出：**
```
v20.x.x
10.x.x
```

### 2.3 安装 pnpm

```bash
npm install -g pnpm

# 验证安装
pnpm --version
```

**预期输出：** `8.x.x` 或更高版本

### 2.4 安装 PM2（进程管理器）

```bash
npm install -g pm2

# 验证安装
pm2 --version
```

**预期输出：** `5.x.x` 或更高版本

### 2.5 安装 Git（如果未安装）

**Ubuntu/Debian:**
```bash
apt install -y git
```

**CentOS/RHEL:**
```bash
yum install -y git
```

**验证安装：**
```bash
git --version
```

---

## 第三步：克隆项目

### 3.1 创建项目目录

```bash
# 创建 projects 目录
mkdir -p ~/projects

# 进入目录
cd ~/projects
```

### 3.2 从 GitHub 克隆项目

```bash
# 克隆仓库
git clone https://github.com/119969788/poly-copy-trading.git

# 进入项目目录
cd poly-copy-trading
```

### 3.3 验证项目结构

```bash
# 查看当前目录
pwd
# 应该显示: /root/projects/poly-copy-trading

# 列出文件
ls -la

# 应该看到以下文件：
# - package.json
# - src/
# - tsconfig.json
# - ecosystem.config.cjs
# 等
```

---

## 第四步：安装项目依赖

### 4.1 使用 pnpm 安装依赖

```bash
# 确保在项目根目录
cd ~/projects/poly-copy-trading

# 安装依赖（可能需要 2-5 分钟）
pnpm install
```

### 4.2 如果遇到网络问题（使用国内镜像）

```bash
# 设置淘宝镜像
pnpm config set registry https://registry.npmmirror.com

# 重新安装
pnpm install
```

### 4.3 验证安装

```bash
# 检查依赖是否安装成功
ls -la node_modules/@catalyst-team/poly-sdk

# 应该看到 SDK 目录存在
```

---

## 第五步：配置环境变量

### 5.1 创建 .env 文件

```bash
# 创建 .env 文件
nano .env
```

### 5.2 添加配置内容

在编辑器中输入以下内容（**请替换为您的真实私钥**）：

```env
# Polymarket 私钥（不要包含 0x 前缀，或包含都可以）
POLYMARKET_PRIVATE_KEY=你的真实私钥

# 可选：指定要跟随的钱包地址（用逗号分隔，如果不设置则跟随排行榜前 50 名）
# TARGET_ADDRESSES=0x1234...,0x5678...

# 可选：设置是否启用模拟模式（true/false，默认 true）
# 首次运行强烈建议设置为 true 进行测试
DRY_RUN=true
```

### 5.3 保存文件

**nano 编辑器操作：**
- 按 `Ctrl + O`（保存）
- 按 `Enter`（确认文件名）
- 按 `Ctrl + X`（退出）

### 5.4 设置文件权限（重要安全措施）

```bash
# 设置 .env 文件权限为 600（只有所有者可读）
chmod 600 .env

# 验证权限
ls -la .env
# 应该显示: -rw------- 1 root root ...
```

### 5.5 验证配置

```bash
# 查看配置（不会显示私钥内容，只显示文件存在）
cat .env | grep -v PRIVATE_KEY
```

---

## 第六步：测试运行

### 6.1 前台测试运行

```bash
# 在项目目录下运行
cd ~/projects/poly-copy-trading
pnpm start
```

**预期输出：**
```
═══════════════════════════════════════════════════
   Polymarket 聪明钱自动跟单系统
═══════════════════════════════════════════════════

🚀 正在初始化 SDK...
✅ SDK 初始化成功
...
```

### 6.2 停止测试

按 `Ctrl + C` 停止运行。

### 6.3 如果测试成功

如果看到 SDK 初始化成功的信息，说明配置正确，可以继续下一步。

---

## 第七步：使用 PM2 后台运行

### 7.1 创建日志目录

```bash
mkdir -p logs
```

### 7.2 使用 PM2 启动应用

```bash
# 使用 ecosystem.config.cjs 配置文件启动（推荐）
pm2 start ecosystem.config.cjs

# 查看状态
pm2 status
```

**预期输出：**
```
┌─────┬──────────────────────┬─────────────┬─────────┬─────────┬──────────┐
│ id  │ name                 │ mode        │ ↺       │ status  │ cpu      │
├─────┼──────────────────────┼─────────────┼─────────┼─────────┼──────────┤
│ 0   │ poly-copy-trading    │ fork        │ 0       │ online  │ 0%       │
└─────┴──────────────────────┴─────────────┴─────────┴─────────┴──────────┘
```

### 7.3 查看日志

```bash
# 查看实时日志
pm2 logs poly-copy-trading

# 查看最后 50 行日志
pm2 logs poly-copy-trading --lines 50

# 实时跟踪日志
pm2 logs poly-copy-trading --follow
```

### 7.4 PM2 常用命令

```bash
# 查看所有应用状态
pm2 status

# 重启应用
pm2 restart poly-copy-trading

# 停止应用
pm2 stop poly-copy-trading

# 删除应用
pm2 delete poly-copy-trading

# 查看详细信息
pm2 show poly-copy-trading

# 实时监控
pm2 monit
```

---

## 第八步：设置开机自启

### 8.1 保存 PM2 配置

```bash
# 保存当前 PM2 进程列表
pm2 save
```

### 8.2 设置开机自启

```bash
# 生成开机自启脚本
pm2 startup
```

**执行后会显示类似这样的命令：**
```
[PM2] Init System found: systemd
[PM2] To setup the Startup Script, copy/paste the following command:
sudo env PATH=$PATH:/usr/bin /usr/lib/node_modules/pm2/bin/pm2 startup systemd -u root --hp /root
```

**复制并执行显示的命令**（根据你的系统会有所不同）

### 8.3 验证开机自启

```bash
# 重启服务器测试（可选，谨慎操作）
# reboot

# 重启后检查
pm2 status
# 应该看到应用自动启动
```

---

## 常见问题解决

### 问题 1：无法连接到服务器

**解决方法：**
- 检查服务器 IP 地址是否正确
- 检查防火墙是否允许 SSH 连接（端口 22）
- 检查服务器是否正在运行
- 如果使用密钥文件，检查密钥文件路径是否正确

### 问题 2：Node.js 安装失败

**解决方法：**
```bash
# 检查网络连接
ping google.com

# 手动下载安装脚本
curl -fsSL https://deb.nodesource.com/setup_20.x -o setup_20.x.sh
bash setup_20.x.sh
apt install -y nodejs
```

### 问题 3：pnpm install 失败

**解决方法：**
```bash
# 使用国内镜像
pnpm config set registry https://registry.npmmirror.com

# 清理缓存
pnpm store prune

# 重新安装
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

### 问题 4：GitHub 克隆失败（网络问题）

**解决方法：**
```bash
# 使用 GitHub 镜像
git clone https://ghproxy.com/https://github.com/119969788/poly-copy-trading.git

# 或使用代理（如果有）
git config --global http.proxy http://your-proxy:port
git config --global https.proxy https://your-proxy:port
```

### 问题 5：应用启动失败

**解决方法：**
```bash
# 查看详细错误日志
pm2 logs poly-copy-trading --err --lines 100

# 检查 .env 文件
cat .env

# 检查私钥格式是否正确（不应该有空格或换行）
# 手动测试运行
cd ~/projects/poly-copy-trading
pnpm start
```

### 问题 6：PM2 应用状态为 errored

**解决方法：**
```bash
# 查看错误详情
pm2 logs poly-copy-trading --err

# 检查 tsx 是否安装
which tsx

# 重新安装依赖
cd ~/projects/poly-copy-trading
rm -rf node_modules
pnpm install

# 重启应用
pm2 restart poly-copy-trading
```

### 问题 7：内存不足

**解决方法：**
```bash
# 查看内存使用
free -h

# 如果内存不足，可以：
# 1. 升级服务器配置
# 2. 或设置 PM2 内存限制自动重启
pm2 restart poly-copy-trading --max-memory-restart 300M
```

### 问题 8：日志文件过大

**解决方法：**
```bash
# 清理日志
pm2 flush

# 或手动清理
rm -f logs/*.log

# 设置日志轮转（在 ecosystem.config.cjs 中已配置）
```

---

## 一键部署脚本（可选）

如果你想要更快速的部署，可以使用项目中的部署脚本：

```bash
# 进入项目目录
cd ~/projects/poly-copy-trading

# 给脚本执行权限
chmod +x deploy.sh

# 运行部署脚本
./deploy.sh
```

脚本会自动：
- 检查并安装 Node.js、pnpm、PM2
- 安装项目依赖
- 检查 .env 文件
- 创建日志目录

**注意：** 脚本不会自动创建 .env 文件，你需要手动创建并配置私钥。

---

## 验证清单

安装完成后，请确认以下项目：

- [ ] Node.js 已安装（`node --version` 显示 v20.x.x）
- [ ] pnpm 已安装（`pnpm --version` 显示 8.x.x）
- [ ] PM2 已安装（`pm2 --version` 显示 5.x.x）
- [ ] 项目已克隆到 `~/projects/poly-copy-trading`
- [ ] 项目依赖已安装（`node_modules` 目录存在）
- [ ] `.env` 文件已创建并配置私钥
- [ ] `.env` 文件权限为 600
- [ ] 测试运行成功（`pnpm start` 能正常启动）
- [ ] PM2 应用状态为 `online`（`pm2 status`）
- [ ] 日志正常输出（`pm2 logs poly-copy-trading`）
- [ ] 开机自启已配置（`pm2 startup` 已执行）

---

## 后续操作

### 更新代码

```bash
cd ~/projects/poly-copy-trading

# 拉取最新代码
git pull origin main

# 安装新依赖（如果有）
pnpm install

# 重启应用
pm2 restart poly-copy-trading
```

### 修改配置

```bash
# 编辑 .env 文件
nano .env

# 重启应用使配置生效
pm2 restart poly-copy-trading
```

### 查看实时日志

```bash
# 实时查看日志
pm2 logs poly-copy-trading --follow

# 查看最后 100 行
pm2 logs poly-copy-trading --lines 100
```

### 停止应用

```bash
# 停止应用
pm2 stop poly-copy-trading

# 删除应用（从 PM2 列表中移除）
pm2 delete poly-copy-trading
```

---

## 安全建议

1. **私钥安全**
   - ✅ 使用 `.env` 文件存储私钥，不要硬编码
   - ✅ 设置 `.env` 文件权限为 600
   - ✅ 不要将 `.env` 文件提交到 Git
   - ✅ 定期备份私钥到安全位置

2. **服务器安全**
   - ✅ 使用 SSH 密钥认证而非密码
   - ✅ 定期更新系统和软件
   - ✅ 配置防火墙规则
   - ✅ 使用非 root 用户运行应用（可选但推荐）

3. **监控和维护**
   - ✅ 定期检查日志
   - ✅ 设置磁盘空间监控
   - ✅ 配置告警通知（可选）

---

## 相关文档

- [服务器首次设置指南](./SERVER_FIRST_TIME_SETUP.md)
- [安装 GitHub 版 SDK 指南](./SERVER_INSTALL_POLY_SDK_DETAILED.md)
- [部署指南](./DEPLOY.md)
- [PM2 设置指南](./PM2_SETUP.md)

---

## 快速参考命令

```bash
# 连接到服务器
ssh root@你的服务器IP

# 克隆项目
cd ~/projects && git clone https://github.com/119969788/poly-copy-trading.git

# 安装依赖
cd ~/projects/poly-copy-trading && pnpm install

# 创建 .env 文件
nano .env

# 测试运行
pnpm start

# PM2 启动
pm2 start ecosystem.config.cjs

# 查看状态
pm2 status

# 查看日志
pm2 logs poly-copy-trading

# 保存配置
pm2 save

# 设置开机自启
pm2 startup
```

---

**💡 提示：** 

1. **首次运行强烈建议设置 `DRY_RUN=true` 进行测试！**
2. 确认一切正常后，再修改 `.env` 文件设置 `DRY_RUN=false` 切换到实盘模式。
3. 定期检查日志，确保应用正常运行。

---

**🎉 安装完成！** 如果遇到任何问题，请参考 [常见问题解决](#常见问题解决) 部分或查看相关文档。
