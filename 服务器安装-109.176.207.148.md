# 🚀 服务器安装指南 - 109.176.207.148

## 📋 服务器信息

- **IP地址**: 109.176.207.148
- **用户名**: root
- **密码**: $Sun3034197*

---

## 🔧 步骤 1：连接到服务器

在本地 PowerShell 或终端执行：

```bash
ssh root@109.176.207.148
```

输入密码：`$Sun3034197*`

---

## 📦 步骤 2：一键安装（推荐）

### 方法 1：使用安装脚本

```bash
# 1. 连接到服务器
ssh root@109.176.207.148

# 2. 下载并运行安装脚本
curl -fsSL https://raw.githubusercontent.com/119969788/poly-copy-trading/main/一键安装脚本.sh -o install.sh
bash install.sh
```

### 方法 2：手动执行安装命令

```bash
# 1. 更新系统
apt update && apt upgrade -y

# 2. 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# 3. 安装 pnpm 和 PM2
npm install -g pnpm pm2

# 4. 安装 Git
apt install -y git

# 5. 克隆仓库
cd /root
git clone https://github.com/119969788/poly-copy-trading.git
cd poly-copy-trading

# 6. 安装依赖
pnpm install

# 7. 创建 .env 文件
cp env.example.txt .env
nano .env  # 编辑并添加私钥
```

---

## ⚙️ 步骤 3：配置环境变量

```bash
cd /root/poly-copy-trading
nano .env
```

在 `.env` 文件中添加：

```env
# 必需：Polymarket 私钥
POLYMARKET_PRIVATE_KEY=your_private_key_here

# 可选：指定要跟随的钱包地址
# TARGET_ADDRESSES=0x1234...,0x5678...

# 可选：是否启用模拟模式（默认 true）
# DRY_RUN=true
```

**重要**：
- 将 `your_private_key_here` 替换为你的实际私钥
- 首次使用建议设置 `DRY_RUN=true`（模拟模式）

保存：`Ctrl+O` → `Enter` → `Ctrl+X`

---

## ✅ 步骤 4：测试运行

```bash
# 测试运行（模拟模式，不会真实交易）
npx tsx src/index.ts
```

按 `Ctrl+C` 停止测试。

---

## 🚀 步骤 5：启动 PM2

```bash
# 启动应用
pm2 start ecosystem.config.cjs
# 或
pm2 start ecosystem.config.js

# 查看状态
pm2 status

# 查看日志
pm2 logs poly-copy-trading
```

---

## 🔄 步骤 6：设置开机自启

```bash
# 保存当前 PM2 进程列表
pm2 save

# 设置开机自启
pm2 startup

# 按照提示执行生成的命令（通常是 sudo 命令）
```

---

## 📊 常用管理命令

```bash
# 查看状态
pm2 status

# 查看日志
pm2 logs poly-copy-trading

# 重启
pm2 restart poly-copy-trading

# 停止
pm2 stop poly-copy-trading
```

---

## 🔐 安全建议

1. **修改 SSH 密码**（可选但推荐）
2. **设置 .env 文件权限**：
   ```bash
   chmod 600 /root/poly-copy-trading/.env
   ```
3. **定期更新代码**：
   ```bash
   cd /root/poly-copy-trading
   git pull origin main
   pnpm install
   pm2 restart poly-copy-trading
   ```

---

## 🆘 故障排查

### 如果 Node.js 安装失败

```bash
# 尝试使用 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc
nvm install 20
nvm use 20
```

### 如果依赖安装失败

```bash
cd /root/poly-copy-trading
rm -rf node_modules
pnpm install
```

### 如果 PM2 启动失败

```bash
# 查看错误日志
pm2 logs poly-copy-trading --err

# 手动测试
npx tsx src/index.ts
```

---

## ✅ 验证安装

```bash
# 检查 Node.js
node --version  # 应该显示 v20.x.x

# 检查 pnpm
pnpm --version

# 检查 PM2
pm2 --version

# 检查进程
pm2 status  # 应该看到 poly-copy-trading
```

---

## 📝 完整安装命令（复制粘贴执行）

```bash
# 连接到服务器后，执行以下命令：

# 1. 更新系统
apt update && apt upgrade -y

# 2. 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# 3. 安装 pnpm 和 PM2
npm install -g pnpm pm2

# 4. 安装 Git
apt install -y git

# 5. 克隆仓库
cd /root
git clone https://github.com/119969788/poly-copy-trading.git
cd poly-copy-trading

# 6. 安装依赖
pnpm install

# 7. 创建 .env 文件
cp env.example.txt .env
nano .env  # 添加私钥后保存

# 8. 测试运行
npx tsx src/index.ts  # 按 Ctrl+C 停止

# 9. 启动 PM2
pm2 start ecosystem.config.cjs
pm2 save
pm2 startup  # 按照提示执行命令
```

---

## 🎯 快速参考

```bash
# 启动
pm2 start ecosystem.config.cjs

# 停止
pm2 stop poly-copy-trading

# 重启
pm2 restart poly-copy-trading

# 查看日志
pm2 logs poly-copy-trading

# 查看状态
pm2 status
```
