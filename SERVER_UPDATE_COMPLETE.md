# 服务器代码完整更新指南

## 📋 需要更新的文件

### 1. 新增文件：`src/batch-sell.ts`
### 2. 更新文件：`package.json`（添加批量出售脚本）

---

## 🔧 更新步骤

### 步骤 1：创建 `src/batch-sell.ts` 文件

在服务器上执行：

```bash
cd ~/poly-copy-trading
mkdir -p src
nano src/batch-sell.ts
```

然后**完整复制**下面的内容并粘贴：
