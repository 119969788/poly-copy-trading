# 服务器代码更新文件

## 需要更新的文件清单

### 1. 新增文件：`src/batch-sell.ts`
### 2. 更新文件：`package.json`
### 3. 更新文件：`src/index.ts`（如果服务器上版本较旧）

---

## 文件 1: src/batch-sell.ts（新增）

在服务器上创建此文件：

```bash
cd ~/poly-copy-trading
mkdir -p src
nano src/batch-sell.ts
```

然后粘贴以下完整内容：
